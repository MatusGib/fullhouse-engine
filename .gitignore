# Internal — kept in separate private repo
portal/
supabase/
matchqueue/
queue/
setup_fullhouse.py

# Python
__pycache__/
*.pyc
*.pyo
.env

# Node
node_modules/

# Generated CFR artifacts — regenerate with training/holdem_*.py. Add with
# `git add -f` and ship in the submission zip only once a blueprint beats the
# heuristic (see training/README.md). The bot runs the heuristic without them.
bots/fullhouse_cfr/data/
